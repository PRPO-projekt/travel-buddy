CREATE TABLE PurchasedTickets (
                         id_racuna UUID PRIMARY KEY,
                         user_id UUID PRIMARY KEY ,
                         ticket_it UUID PRIMARY KEY,
                         stanje_racuna INT
);